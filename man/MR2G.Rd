% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/main.R
\name{MR2G}
\alias{MR2G}
\title{Main MR2G analysis}
\usage{
MR2G(
  b_mat,
  se_mat,
  n_vec,
  rho_mat,
  IV_list,
  R_list,
  pval_mat = NULL,
  n_mat = NULL,
  trait_vec = NULL,
  IV_screen = "ScreenAug",
  pthres = 0.05/((length(n_vec))^2 - length(n_vec)),
  random_start = 0,
  num_pert_M = 100,
  num_pert = 100,
  seed = 0,
  check = TRUE
)
}
\arguments{
\item{b_mat}{Matrix of SNP-trait effects.}

\item{se_mat}{Matrix of standard errors corresponding to \code{b_mat}.}

\item{n_vec}{Vector of per-trait sample sizes.}

\item{rho_mat}{Trait correlation matrix.}

\item{IV_list}{List of IVs per trait pair.}

\item{R_list}{LD / correlation structure list.}

\item{pval_mat}{Optional matrix of p-values (same shape as \code{b_mat}).}

\item{n_mat}{Optional matrix of sample sizes per SNP-trait pair (same shape as \code{b_mat}).}

\item{trait_vec}{Optional vector of trait names.}

\item{IV_screen}{One of \code{"ScreenAug"}, \code{"ScreenMax"}, or \code{"ScreenPair"}.}

\item{pthres}{P-value threshold for edge selection in ScreenMax result when \code{IV_screen='ScreenAug'}.}

\item{random_start}{Number of random starts for optimization in \code{mr_cML_O}.}

\item{num_pert_M}{Number of perturbations used for ScreenMax/ScreenAug phases.}

\item{num_pert}{Number of perturbations for final result phase.}

\item{seed}{Random seed.}

\item{check}{Logical; Whether or not to exclude perturbation results with spectral radius greater than 1.}
}
\value{
A list (shape depends on IV_screen) of causal graph results.
}
\description{
Main MR2G analysis
}
\examples{
# Load example data
data("example", package = "MR2G")
# Run MR2G on the example data (with small num_pert for quick execution)
result <- MR2G(
  b_mat   = b_mat,
  se_mat  = se_mat,
  n_vec   = n_vec,
  rho_mat = rho_mat,
  IV_list = IV_list,
  R_list  = R_list,
  num_pert = 5, num_pert_M = 5  # small test run
)
str(result)

}
